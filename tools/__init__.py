"""Development-time tooling for the yate repository.

This package is a developer aid only; it is NOT part of the ``yate`` runtime
package and is never packaged into the wheel (hatchling only ships ``yate/``).
"""
